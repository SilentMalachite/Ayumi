# DBバックアップ機能 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SQLite DBを `VACUUM INTO` で安全にバックアップする機能を追加する（管理者UI＋mixタスクの2経路）

**Architecture:** コンテキストモジュール `Ayumi.Backups` に `create_backup/2` を実装し、`VACUUM INTO` で一貫したスナップショットを生成する。UIは管理者専用の LiveView `/admin/backup`、CLIは `mix ayumi.backup PATH` で同じコンテキスト関数を呼ぶ。

**Tech Stack:** Elixir, Phoenix LiveView, Ecto (ecto_sqlite3/exqlite), SQLite `VACUUM INTO`

## Global Constraints

- `VACUUM INTO` のみ使用。`File.cp` によるDB直接コピーは禁止
- 新規依存の追加なし
- 最小diff。既存機能を壊さない
- 完了条件：`mix test` 緑、かつ `mix review`（compile / Credo / Sobelow / Dialyzer）クリーン
- コード識別子・コメントは英語、UIテキストは日本語（gettext）
- バックアップは管理者（`role: "manager"`）のみ実行可能

---

## File Structure

| Action | Path | Responsibility |
|--------|------|----------------|
| Create | `lib/ayumi/backups.ex` | コンテキスト：パス検証、`VACUUM INTO` 実行、結果返却 |
| Create | `lib/mix/tasks/ayumi.backup.ex` | mixタスク：CLIエントリポイント |
| Create | `lib/ayumi_web/live/backup_live/index.ex` | LiveView：管理者向けバックアップUI |
| Modify | `lib/ayumi_web/router.ex` | `/admin/backup` ルート追加（`:require_manager` セッション内） |
| Modify | `config/config.exs` | `config :ayumi, :backup_dir` デフォルト値 |
| Create | `test/ayumi/backups_test.exs` | コンテキスト関数の単体テスト |
| Create | `test/ayumi_web/live/backup_live_test.exs` | LiveView統合テスト |

---

### Task 1: `Ayumi.Backups` コンテキスト — パス検証＋ `VACUUM INTO` 実行

**Files:**
- Create: `lib/ayumi/backups.ex`
- Create: `test/ayumi/backups_test.exs`
- Modify: `config/config.exs` (末尾の `import_config` 行の直前に1行追加)

**Interfaces:**
- Consumes: `Ayumi.Repo` (Ecto repo, SQLite3 adapter)
- Produces: `Ayumi.Backups.create_backup(dest_dir, opts \\ [])` → `{:ok, %{path: String.t(), size_bytes: non_neg_integer(), created_at: DateTime.t()}}` | `{:error, String.t()}`

- [ ] **Step 1: config にデフォルトバックアップディレクトリを追加**

`config/config.exs` の末尾（`import_config` 行の直前）に以下を追加:

```elixir
config :ayumi, :backup_dir, Path.expand("../backups", __DIR__)
```

- [ ] **Step 2: 失敗テストを書く — 存在しないディレクトリ**

`test/ayumi/backups_test.exs` を作成:

```elixir
defmodule Ayumi.BackupsTest do
  use Ayumi.DataCase, async: false

  alias Ayumi.Backups

  describe "create_backup/2" do
    test "returns error for non-existent directory" do
      assert {:error, reason} = Backups.create_backup("/tmp/ayumi_no_such_dir_#{System.unique_integer()}")
      assert reason =~ "存在しない" or reason =~ "not found" or reason =~ "does not exist"
    end
  end
end
```

- [ ] **Step 3: テストが失敗することを確認**

Run: `mix test test/ayumi/backups_test.exs`
Expected: FAIL — `Ayumi.Backups` モジュールが存在しない

- [ ] **Step 4: `Ayumi.Backups` の骨格を実装**

`lib/ayumi/backups.ex` を作成:

```elixir
defmodule Ayumi.Backups do
  @moduledoc false

  alias Ayumi.Repo

  @doc """
  Creates a consistent SQLite backup using VACUUM INTO.

  Returns `{:ok, info}` with the backup path, size, and timestamp,
  or `{:error, reason}` on failure.
  """
  def create_backup(dest_dir, opts \\ []) do
    with :ok <- validate_directory(dest_dir),
         :ok <- validate_not_self(dest_dir),
         {:ok, dest_path} <- build_dest_path(dest_dir, opts),
         :ok <- execute_vacuum_into(dest_path) do
      stat = File.stat!(dest_path)
      {:ok, %{path: dest_path, size_bytes: stat.size, created_at: DateTime.utc_now()}}
    end
  end

  defp validate_directory(dir) do
    cond do
      not File.dir?(dir) ->
        {:error, "指定されたディレクトリが存在しません: #{dir}"}

      not writable?(dir) ->
        {:error, "ディレクトリに書き込み権限がありません: #{dir}"}

      true ->
        :ok
    end
  end

  defp writable?(dir) do
    probe = Path.join(dir, ".ayumi_write_probe_#{System.unique_integer([:positive])}")

    case File.touch(probe) do
      :ok ->
        File.rm(probe)
        true

      {:error, _} ->
        false
    end
  end

  defp validate_not_self(dest_dir) do
    db_path = Repo.config()[:database] |> Path.expand() |> Path.dirname()
    dest_abs = Path.expand(dest_dir)

    if dest_abs == db_path do
      {:error, "バックアップ先が稼働中DBと同じディレクトリです。別のディレクトリを指定してください"}
    else
      :ok
    end
  end

  defp build_dest_path(dest_dir, opts) do
    now = Keyword.get(opts, :timestamp, NaiveDateTime.utc_now())

    filename =
      "ayumi_backup_" <>
        Calendar.strftime(now, "%Y%m%d_%H%M%S") <>
        ".sqlite3"

    {:ok, Path.join(dest_dir, filename)}
  end

  defp execute_vacuum_into(dest_path) do
    safe_path = String.replace(dest_path, "'", "''")
    sql = "VACUUM INTO '#{safe_path}'"

    case Ecto.Adapters.SQL.query(Repo, sql, []) do
      {:ok, _} -> :ok
      {:error, err} -> {:error, "VACUUM INTO failed: #{inspect(err)}"}
    end
  end
end
```

- [ ] **Step 5: 存在しないディレクトリのテストが通ることを確認**

Run: `mix test test/ayumi/backups_test.exs`
Expected: PASS

- [ ] **Step 6: 正常系テストを追加 — バックアップ成功＋ファイル読み取り検証**

`test/ayumi/backups_test.exs` の `describe` 内に追加:

```elixir
    @tag :tmp_dir
    test "creates a valid backup file in the target directory", %{tmp_dir: tmp_dir} do
      assert {:ok, info} = Backups.create_backup(tmp_dir)
      assert File.exists?(info.path)
      assert info.size_bytes > 0
      assert String.starts_with?(Path.basename(info.path), "ayumi_backup_")
      assert String.ends_with?(info.path, ".sqlite3")

      # Verify the backup is a readable SQLite database with expected tables
      {:ok, conn} = Exqlite.Sqlite3.open(info.path)

      {:ok, stmt} =
        Exqlite.Sqlite3.prepare(conn, "SELECT name FROM sqlite_master WHERE type='table' ORDER BY name")

      tables = collect_rows(stmt, conn)
      Exqlite.Sqlite3.close(conn)

      assert "users" in tables
      assert "support_plans" in tables
    end

    defp collect_rows(stmt, conn, acc \\ []) do
      case Exqlite.Sqlite3.step(conn, stmt) do
        {:row, [value]} -> collect_rows(stmt, conn, [value | acc])
        :done -> Enum.reverse(acc)
      end
    end
```

- [ ] **Step 7: テストが通ることを確認**

Run: `mix test test/ayumi/backups_test.exs`
Expected: PASS

- [ ] **Step 8: エッジケーステストを追加 — 自己上書き防止**

`test/ayumi/backups_test.exs` の `describe` 内に追加:

```elixir
    test "returns error when dest_dir is the same as the running DB directory" do
      db_dir = Repo.config()[:database] |> Path.expand() |> Path.dirname()
      assert {:error, reason} = Backups.create_backup(db_dir)
      assert reason =~ "同じディレクトリ"
    end
```

- [ ] **Step 9: テストが通ることを確認**

Run: `mix test test/ayumi/backups_test.exs`
Expected: PASS

- [ ] **Step 10: Commit**

```bash
git add lib/ayumi/backups.ex test/ayumi/backups_test.exs config/config.exs
git commit -m "feat: add Ayumi.Backups context with VACUUM INTO backup"
```

---

### Task 2: `mix ayumi.backup` mixタスク

**Files:**
- Create: `lib/mix/tasks/ayumi.backup.ex`

**Interfaces:**
- Consumes: `Ayumi.Backups.create_backup/1`
- Produces: `mix ayumi.backup PATH` CLI command（成功→パスとサイズ出力、失敗→非ゼロ終了）

**Ref:** 既存の `lib/mix/tasks/ayumi.create_user.ex` の `Mix.Tasks.Ayumi.CreateUser` を構造の参考にする

- [ ] **Step 1: mixタスクを実装**

`lib/mix/tasks/ayumi.backup.ex` を作成:

```elixir
defmodule Mix.Tasks.Ayumi.Backup do
  @shortdoc "Creates a SQLite backup via VACUUM INTO"
  @moduledoc """
  Creates a consistent backup of the Ayumi SQLite database.

      mix ayumi.backup PATH

  PATH is the directory where the backup file will be saved.
  The filename is generated automatically with a timestamp.

  ## Examples

      mix ayumi.backup /var/backups/ayumi
      mix ayumi.backup ./backups

  """
  use Mix.Task

  @requirements ["app.start"]

  @impl Mix.Task
  def run([dest_dir]) do
    case Ayumi.Backups.create_backup(dest_dir) do
      {:ok, info} ->
        size_kb = div(info.size_bytes, 1024)

        Mix.shell().info("""
        バックアップが完了しました。
          パス: #{info.path}
          サイズ: #{size_kb} KB
        """)

      {:error, reason} ->
        Mix.shell().error("バックアップに失敗しました: #{reason}")
        exit({:shutdown, 1})
    end
  end

  def run(_) do
    Mix.shell().error("使い方: mix ayumi.backup <保存先ディレクトリ>")
    exit({:shutdown, 1})
  end
end
```

- [ ] **Step 2: 動作確認（手動）**

Run: `mkdir -p /tmp/ayumi_backup_test && mix ayumi.backup /tmp/ayumi_backup_test`
Expected: バックアップ成功メッセージ＋ファイル生成

- [ ] **Step 3: Commit**

```bash
git add lib/mix/tasks/ayumi.backup.ex
git commit -m "feat: add mix ayumi.backup task"
```

---

### Task 3: 管理者向けバックアップ LiveView UI

**Files:**
- Create: `lib/ayumi_web/live/backup_live/index.ex`
- Modify: `lib/ayumi_web/router.ex:48-55` (`:require_manager` live_session 内にルート追加)
- Modify: `lib/ayumi_web/components/layouts.ex:46-48` (ナビバーに管理者向けリンク追加)
- Create: `test/ayumi_web/live/backup_live_test.exs`

**Interfaces:**
- Consumes: `Ayumi.Backups.create_backup/1`, `Ayumi.Accounts.Scope.manager?/1`
- Produces: `/admin/backup` ページ（保存先入力、実行ボタン、結果Flash表示）

- [ ] **Step 1: LiveViewの認可テストを書く**

`test/ayumi_web/live/backup_live_test.exs` を作成:

```elixir
defmodule AyumiWeb.BackupLiveTest do
  use AyumiWeb.ConnCase, async: false

  import Phoenix.LiveViewTest
  import Ayumi.AccountsFixtures

  describe "GET /admin/backup" do
    test "redirects non-manager users", %{conn: conn} do
      user = user_fixture(%{role: "supporter"})
      conn = log_in_user(conn, user)
      {:error, {:live_redirect, redirect}} = live(conn, ~p"/admin/backup")
      assert redirect.to == "/"
    end

    test "renders for manager users", %{conn: conn} do
      user = user_fixture(%{role: "manager"})
      conn = log_in_user(conn, user)
      {:ok, _lv, html} = live(conn, ~p"/admin/backup")
      assert html =~ "バックアップ"
    end
  end

  describe "backup execution" do
    setup %{conn: conn} do
      user = user_fixture(%{role: "manager"})
      conn = log_in_user(conn, user)
      %{conn: conn}
    end

    @tag :tmp_dir
    test "creates backup successfully", %{conn: conn, tmp_dir: tmp_dir} do
      {:ok, lv, _html} = live(conn, ~p"/admin/backup")

      html =
        lv
        |> form("#backup-form", %{dest_dir: tmp_dir})
        |> render_submit()

      assert html =~ "バックアップが完了しました" or html =~ "ayumi_backup_"
    end

    test "shows error for invalid directory", %{conn: conn} do
      {:ok, lv, _html} = live(conn, ~p"/admin/backup")

      html =
        lv
        |> form("#backup-form", %{dest_dir: "/nonexistent/path/#{System.unique_integer()}"})
        |> render_submit()

      assert html =~ "存在しない" or html =~ "失敗"
    end
  end
end
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `mix test test/ayumi_web/live/backup_live_test.exs`
Expected: FAIL — ルートが存在しない

- [ ] **Step 3: ルーターにルートを追加**

`lib/ayumi_web/router.ex` の `:require_manager` live_session 内（`live "/service_users/:service_user_id/support_plans/new"` の行の直後）に追加:

```elixir
      live "/admin/backup", BackupLive.Index, :index
```

- [ ] **Step 4: LiveViewを実装**

`lib/ayumi_web/live/backup_live/index.ex` を作成:

```elixir
defmodule AyumiWeb.BackupLive.Index do
  use AyumiWeb, :live_view

  alias Ayumi.Backups

  @impl true
  def mount(_params, _session, socket) do
    default_dir = Application.get_env(:ayumi, :backup_dir, "")

    {:ok,
     socket
     |> assign(:page_title, gettext("データベースバックアップ"))
     |> assign(:dest_dir, default_dir)
     |> assign(:result, nil)}
  end

  @impl true
  def handle_event("backup", %{"dest_dir" => dest_dir}, socket) do
    case Backups.create_backup(dest_dir) do
      {:ok, info} ->
        size_kb = div(info.size_bytes, 1024)

        {:noreply,
         socket
         |> assign(:result, {:ok, info})
         |> put_flash(:info, gettext("バックアップが完了しました: %{path} (%{size} KB)", path: info.path, size: size_kb))}

      {:error, reason} ->
        {:noreply,
         socket
         |> assign(:result, {:error, reason})
         |> put_flash(:error, gettext("バックアップに失敗しました: %{reason}", reason: reason))}
    end
  end

  @impl true
  def render(assigns) do
    ~H"""
    <.header>
      {gettext("データベースバックアップ")}
    </.header>

    <div class="mt-6">
      <form id="backup-form" phx-submit="backup" class="space-y-4">
        <div>
          <label for="dest_dir" class="block text-sm font-medium">
            {gettext("保存先ディレクトリ")}
          </label>
          <input
            type="text"
            name="dest_dir"
            id="dest_dir"
            value={@dest_dir}
            class="input input-bordered w-full mt-1"
            placeholder="/path/to/backup/directory"
            required
          />
          <p class="mt-1 text-sm text-base-content/60">
            {gettext("バックアップファイルの保存先を指定してください。タイムスタンプ付きのファイル名が自動生成されます。")}
          </p>
        </div>

        <button type="submit" class="btn btn-primary">
          {gettext("バックアップ実行")}
        </button>
      </form>

      <div :if={match?({:ok, _}, @result)} class="mt-6 alert alert-success">
        <div>
          <p class="font-semibold">{gettext("バックアップ完了")}</p>
          <p class="text-sm">{elem(@result, 1).path}</p>
          <p class="text-sm">{div(elem(@result, 1).size_bytes, 1024)} KB</p>
        </div>
      </div>
    </div>
    """
  end
end
```

- [ ] **Step 5: テストが通ることを確認**

Run: `mix test test/ayumi_web/live/backup_live_test.exs`
Expected: PASS

- [ ] **Step 6: ナビバーに管理者向けリンクを追加（任意）**

`lib/ayumi_web/components/layouts.ex` の `app/1` 関数内、`利用者` リンクの `</li>` の直後に追加:

```heex
          <li :if={Ayumi.Accounts.Scope.manager?(@current_scope)}>
            <.link navigate={~p"/admin/backup"} class="font-semibold">{gettext("バックアップ")}</.link>
          </li>
```

**注意:** `@current_scope` が `app/1` の assigns に含まれているか確認。含まれていない場合はこのステップをスキップし、`/admin/backup` への直接URLアクセスのみとする。

- [ ] **Step 7: Commit**

```bash
git add lib/ayumi_web/live/backup_live/index.ex lib/ayumi_web/router.ex test/ayumi_web/live/backup_live_test.exs
git commit -m "feat: add manager-only backup LiveView at /admin/backup"
```

ナビバーも変更した場合:

```bash
git add lib/ayumi_web/components/layouts.ex
git commit --amend --no-edit
```

---

### Task 4: 品質ゲート＋最終検証

**Files:**
- 全ファイル（変更なし、検証のみ）

**Interfaces:**
- Consumes: 全タスクの成果物
- Produces: `mix review` クリーンな状態

- [ ] **Step 1: 全テスト実行**

Run: `mix test`
Expected: 全テスト PASS

- [ ] **Step 2: `mix review` 実行**

Run: `mix review`
Expected: format, compile, credo, sobelow, dialyzer すべて PASS

Sobelow が `VACUUM INTO` の SQL 文字列連結について `SQL Injection` を指摘する可能性がある。その場合:
- ファイル名はアプリ生成（タイムスタンプのみ）で外部入力を含まない
- ディレクトリパスは `validate_directory/1` で実在確認済み
- シングルクオートのエスケープ済み

上記の理由で安全と判断できる場合、`# sobelow_skip ["SQL.Query"]` を `execute_vacuum_into/1` の直前に追加する。

- [ ] **Step 3: 手動動作確認（ブラウザ）**

1. `mix phx.server` でサーバー起動
2. manager ユーザーでログイン
3. `/admin/backup` にアクセス
4. 任意のディレクトリ（例: `/tmp/ayumi_backups`）を入力して実行
5. 成功メッセージとファイルパスが表示されることを確認
6. supporter ユーザーでログインし、`/admin/backup` にアクセスするとリダイレクトされることを確認

- [ ] **Step 4: Commit（必要な修正があれば）**

```bash
git add -A
git commit -m "fix: address mix review findings for backup feature"
```

---

## 受け入れ基準チェックリスト

- [ ] `VACUUM INTO` で一貫したバックアップが作られる（`File.cp` を使っていない）
- [ ] 任意の保存先ディレクトリを指定でき、タイムスタンプ付きファイルが作られる
- [ ] ディレクトリ検証（実在・書込可・自己上書き禁止）がある
- [ ] 管理者のみ実行できる
- [ ] `mix ayumi.backup PATH` で同じことができ、失敗時は非ゼロ終了
- [ ] 生成物が実際に開けて読めることをテストで確認している
- [ ] `mix review` クリーン

## リストア手順（参考・実装しない）

アプリを停止 → バックアップ `.sqlite3` を本番DBのパスへ置き換え → 再起動。
